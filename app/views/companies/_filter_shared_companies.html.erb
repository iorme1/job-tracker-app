<%= form_tag company_shared_index_path, method: "get" do %>
  <%= select_tag "status", options_for_select(["pending", "rejected", "inquired"]) %>
  <%= submit_tag "Filter Companies", name: nil, class: "btn btn-primary" %>
<% end %>
